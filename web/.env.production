# Baked into the bundle by `vite build`. Without it DEFAULT_URL falls back to
# location.hostname:8765, which on the deployed site points at a port
# Cloudflare does not serve, so nothing can connect.
VITE_SERVER_URL=wss://indiscreet-chess-valaisu.fly.dev
